#!/usr/bin/expect -f 
set timeout 1200
set des_hostname [lindex $argv 0]
set password [lindex $argv 1]
spawn /usr/bin/scp -r /var/sn/ $des_hostname:/var/
expect {

 "password*" {set timeout 300; send "$password\r"; }
 "yes/no" {send "yes\r"; exp_continue;}
 }
expect eof
